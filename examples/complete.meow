// # Meow examples
// This is a list of expressions that Meow is able to perform.
// You can actually compile that into a .class and run it in the JVM.

// ## defining values
// All values in Meow can be defined with the 'let' keyword like this:

let x = 1
let y = 42

// ## printing to stdout
// In Meow `printLine()` is the equivalent to `System.out.println()` in Java.

printLine(900)
printLine(4000)
